/* ============================================
   HUB DE PROJETOS · KELLY ALMEIDA
   Dark Executive UI
   ============================================ */

*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

:root {
  --bg-base:       #0a0a0f;
  --bg-surface:    #111118;
  --bg-card:       #16161f;
  --bg-card-hover: #1c1c28;
  --bg-elevated:   #1e1e2a;
  --border:        rgba(255,255,255,0.07);
  --border-light:  rgba(255,255,255,0.04);
  --text-primary:  #f1f1f5;
  --text-secondary:#94949f;
  --text-muted:    #5c5c6b;
  --accent-red:    #ef4444;
  --accent-orange: #f97316;
  --accent-blue:   #3b82f6;
  --accent-purple: #a855f7;
  --accent-green:  #22c55e;
  --accent-yellow: #fbbf24;
  --sidebar-w:     240px;
  --topbar-h:      56px;
  --radius:        12px;
  --radius-sm:     8px;
  --transition:    0.18s ease;
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
}

html, body {
  background: var(--bg-base);
  color: var(--text-primary);
  min-height: 100vh;
  font-size: 14px;
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
}

/* ============================================
   SCROLLBAR
   ============================================ */
::-webkit-scrollbar { width: 6px; height: 6px; }
::-webkit-scrollbar-track { background: transparent; }
::-webkit-scrollbar-thumb { background: rgba(255,255,255,0.1); border-radius: 3px; }

/* ============================================
   LAYOUT
   ============================================ */
.mobile-topbar {
  display: none;
  position: fixed;
  top: 0; left: 0; right: 0;
  height: var(--topbar-h);
  background: var(--bg-surface);
  border-bottom: 1px solid var(--border);
  align-items: center;
  padding: 0 16px;
  gap: 12px;
  z-index: 100;
}

.menu-toggle {
  background: none;
  border: none;
  color: var(--text-primary);
  font-size: 18px;
  cursor: pointer;
  padding: 6px;
  border-radius: 6px;
  transition: background var(--transition);
}
.menu-toggle:hover { background: rgba(255,255,255,0.07); }

.mobile-title {
  flex: 1;
  font-size: 15px;
  font-weight: 600;
  color: var(--text-primary);
}

.mobile-avatar {
  width: 32px; height: 32px;
  background: var(--accent-red);
  border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  font-size: 13px; font-weight: 700; color: #fff;
}

.sidebar-overlay {
  display: none;
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,0.6);
  z-index: 200;
}
.sidebar-overlay.active { display: block; }

.sidebar {
  position: fixed;
  top: 0; left: 0;
  width: var(--sidebar-w);
  height: 100vh;
  background: var(--bg-surface);
  border-right: 1px solid var(--border);
  display: flex;
  flex-direction: column;
  z-index: 300;
  overflow-y: auto;
}

.sidebar-brand {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 20px 16px 16px;
  border-bottom: 1px solid var(--border);
}

.brand-avatar {
  width: 36px; height: 36px;
  background: linear-gradient(135deg, #ef4444, #c026d3);
  border-radius: 10px;
  display: flex; align-items: center; justify-content: center;
  font-size: 15px; font-weight: 800; color: #fff;
  flex-shrink: 0;
}

.brand-name {
  display: block;
  font-size: 13px;
  font-weight: 700;
  color: var(--text-primary);
  line-height: 1.2;
}

.brand-role {
  display: block;
  font-size: 11px;
  color: var(--text-muted);
}

.sidebar-nav {
  flex: 1;
  padding: 12px 8px;
  display: flex;
  flex-direction: column;
  gap: 2px;
}

.nav-section-label {
  font-size: 10px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--text-muted);
  padding: 12px 8px 4px;
}

.nav-item {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 8px 10px;
  border: none;
  background: none;
  color: var(--text-secondary);
  border-radius: var(--radius-sm);
  cursor: pointer;
  width: 100%;
  text-align: left;
  font-size: 13px;
  font-weight: 500;
  transition: all var(--transition);
  position: relative;
}

.nav-item i {
  width: 16px;
  text-align: center;
  font-size: 13px;
  flex-shrink: 0;
}

.nav-item:hover {
  background: rgba(255,255,255,0.05);
  color: var(--text-primary);
}

.nav-item.active {
  background: rgba(239,68,68,0.1);
  color: var(--accent-red);
}

.nav-item span:first-of-type { flex: 1; }

.nav-dot {
  width: 8px; height: 8px;
  border-radius: 50%;
  flex-shrink: 0;
}

.nav-badge {
  font-size: 10px;
  font-weight: 600;
  padding: 2px 7px;
  border-radius: 20px;
  flex-shrink: 0;
}

.active-badge {
  background: rgba(239,68,68,0.15);
  color: var(--accent-red);
}

.next-badge {
  background: rgba(249,115,22,0.15);
  color: var(--accent-orange);
}

.sidebar-footer {
  padding: 16px;
  border-top: 1px solid var(--border);
}

.date-display {
  font-size: 11px;
  color: var(--text-muted);
  line-height: 1.5;
}

.main-content {
  margin-left: var(--sidebar-w);
  min-height: 100vh;
  padding: 32px;
}

/* ============================================
   VIEWS
   ============================================ */
.view { display: none; animation: fadeIn 0.2s ease; }
.view.active { display: block; }

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(6px); }
  to   { opacity: 1; transform: translateY(0); }
}

/* ============================================
   PAGE HEADER
   ============================================ */
.page-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 16px;
  margin-bottom: 28px;
}

.page-eyebrow {
  font-size: 11px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  color: var(--text-muted);
  margin-bottom: 4px;
}

.page-title {
  font-size: 26px;
  font-weight: 800;
  color: var(--text-primary);
  letter-spacing: -0.02em;
  line-height: 1.2;
}

.page-subtitle {
  font-size: 13px;
  color: var(--text-secondary);
  margin-top: 4px;
}

/* ============================================
   BTN
   ============================================ */
.btn-primary {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 9px 18px;
  background: var(--accent-red);
  color: #fff;
  border: none;
  border-radius: var(--radius-sm);
  font-size: 13px;
  font-weight: 600;
  cursor: pointer;
  white-space: nowrap;
  transition: all var(--transition);
}
.btn-primary:hover { background: #dc2626; transform: translateY(-1px); }

/* ============================================
   FOCUS BANNER
   ============================================ */
.focus-banner {
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: linear-gradient(135deg, rgba(239,68,68,0.15) 0%, rgba(239,68,68,0.05) 100%);
  border: 1px solid rgba(239,68,68,0.25);
  border-radius: var(--radius);
  padding: 20px 24px;
  margin-bottom: 28px;
  cursor: pointer;
  transition: all var(--transition);
}
.focus-banner:hover {
  background: linear-gradient(135deg, rgba(239,68,68,0.2) 0%, rgba(239,68,68,0.08) 100%);
  border-color: rgba(239,68,68,0.4);
  transform: translateY(-1px);
}

.focus-tag {
  font-size: 10px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  color: var(--accent-red);
  display: block;
  margin-bottom: 4px;
}

.focus-title {
  font-size: 22px;
  font-weight: 800;
  color: var(--text-primary);
  margin-bottom: 4px;
}

.focus-desc {
  font-size: 13px;
  color: var(--text-secondary);
}

.focus-right {
  display: flex;
  align-items: center;
  gap: 16px;
}

.focus-progress-ring {
  position: relative;
  width: 56px; height: 56px;
}

.focus-progress-ring svg {
  width: 100%; height: 100%;
}

.focus-progress-ring span {
  position: absolute;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 12px;
  font-weight: 700;
  color: var(--text-primary);
}

.focus-arrow {
  font-size: 18px;
  color: var(--text-muted);
}

/* ============================================
   PROJECTS GRID
   ============================================ */
.projects-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
  gap: 16px;
}

.project-card {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: 20px;
  cursor: pointer;
  transition: all var(--transition);
  position: relative;
  overflow: hidden;
}

.project-card:hover {
  background: var(--bg-card-hover);
  border-color: rgba(255,255,255,0.12);
  transform: translateY(-2px);
  box-shadow: 0 8px 32px rgba(0,0,0,0.3);
}

.card-accent {
  position: absolute;
  top: 0; left: 0; right: 0;
  height: 3px;
}

.yassuo-accent    { background: var(--accent-red); }
.revesttudo-accent{ background: var(--accent-orange); }
.zevendas-accent  { background: var(--accent-blue); }
.aime-accent      { background: var(--accent-purple); }

.card-header {
  display: flex;
  align-items: center;
  gap: 12px;
  margin-bottom: 12px;
}

.card-icon {
  width: 38px; height: 38px;
  border-radius: 10px;
  display: flex; align-items: center; justify-content: center;
  font-size: 16px;
  flex-shrink: 0;
}

.yassuo-icon    { background: rgba(239,68,68,0.12);  color: var(--accent-red); }
.revesttudo-icon{ background: rgba(249,115,22,0.12); color: var(--accent-orange); }
.zevendas-icon  { background: rgba(59,130,246,0.12); color: var(--accent-blue); }
.aime-icon      { background: rgba(168,85,247,0.12); color: var(--accent-purple); }

.card-meta { flex: 1; min-width: 0; }

.card-title {
  font-size: 15px;
  font-weight: 700;
  color: var(--text-primary);
}

.card-category {
  font-size: 11px;
  color: var(--text-muted);
}

.card-desc {
  font-size: 13px;
  color: var(--text-secondary);
  line-height: 1.55;
  margin-bottom: 12px;
}

.card-tags {
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
  margin-bottom: 16px;
}

.tag {
  font-size: 11px;
  font-weight: 600;
  padding: 3px 9px;
  border-radius: 20px;
}

.tag-red    { background: rgba(239,68,68,0.12);  color: #fca5a5; }
.tag-orange { background: rgba(249,115,22,0.12); color: #fdba74; }
.tag-blue   { background: rgba(59,130,246,0.12); color: #93c5fd; }
.tag-purple { background: rgba(168,85,247,0.12); color: #d8b4fe; }
.tag-gray   { background: rgba(255,255,255,0.06); color: var(--text-secondary); }

.card-footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
}

.next-step {
  display: flex;
  align-items: center;
  gap: 6px;
  font-size: 12px;
  color: var(--text-muted);
}

.next-step i { font-size: 10px; }

.card-progress { flex: 1; max-width: 80px; }

.progress-bar {
  height: 4px;
  background: rgba(255,255,255,0.07);
  border-radius: 2px;
  overflow: hidden;
}

.progress-fill {
  height: 100%;
  border-radius: 2px;
  transition: width 0.6s ease;
}

/* ============================================
   STATUS BADGES
   ============================================ */
.status-badge {
  font-size: 11px;
  font-weight: 600;
  padding: 3px 9px;
  border-radius: 20px;
  white-space: nowrap;
}

.status-badge-lg {
  font-size: 12px;
  font-weight: 700;
  padding: 6px 14px;
  border-radius: 20px;
  white-space: nowrap;
  flex-shrink: 0;
}

.status-active  { background: rgba(34,197,94,0.12);  color: #4ade80; }
.status-next    { background: rgba(249,115,22,0.12); color: #fb923c; }
.status-building{ background: rgba(59,130,246,0.12); color: #60a5fa; }
.status-concept { background: rgba(168,85,247,0.12); color: #c084fc; }

/* ============================================
   PRIORITY COLUMNS
   ============================================ */
.priority-columns {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.priority-col {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  overflow: hidden;
}

.priority-col-header {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 14px 16px;
  font-size: 13px;
  font-weight: 700;
  border-bottom: 1px solid var(--border);
}

.now-header  { color: var(--accent-red); }
.next-header { color: var(--accent-orange); }
.future-header { color: var(--text-secondary); }

.col-count {
  margin-left: auto;
  background: rgba(255,255,255,0.06);
  color: var(--text-muted);
  font-size: 11px;
  padding: 2px 8px;
  border-radius: 20px;
}

.priority-items {
  padding: 8px;
  display: flex;
  flex-direction: column;
  gap: 2px;
}

.priority-item {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 10px 12px;
  border-radius: var(--radius-sm);
  cursor: pointer;
  transition: background var(--transition);
}

.priority-item:hover { background: rgba(255,255,255,0.04); }

.priority-dot {
  width: 8px; height: 8px;
  border-radius: 50%;
  flex-shrink: 0;
}

.dot-red    { background: var(--accent-red); }
.dot-orange { background: var(--accent-orange); }
.dot-yellow { background: var(--accent-yellow); }
.dot-blue   { background: var(--accent-blue); }
.dot-purple { background: var(--accent-purple); }
.dot-gray   { background: var(--text-muted); }

.priority-content { flex: 1; }

.priority-project {
  display: block;
  font-size: 10px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.06em;
  color: var(--text-muted);
  margin-bottom: 2px;
}

.priority-task {
  font-size: 12px;
  color: var(--text-secondary);
}

.prio-arrow {
  font-size: 10px;
  color: var(--text-muted);
}

/* ============================================
   METRICS ROW
   ============================================ */
.metrics-row {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 12px;
  margin-bottom: 28px;
}

.metric-card {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: 16px 18px;
  display: flex;
  align-items: center;
  gap: 14px;
}

.metric-icon {
  font-size: 20px;
  flex-shrink: 0;
}

.metric-value {
  font-size: 20px;
  font-weight: 800;
  color: var(--text-primary);
  line-height: 1;
}

.metric-label {
  font-size: 11px;
  color: var(--text-muted);
  margin-top: 3px;
}

/* ============================================
   SECTION TITLE
   ============================================ */
.section-title-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 14px;
}

.section-title {
  font-size: 16px;
  font-weight: 700;
  color: var(--text-primary);
}

/* ============================================
   MODULES GRID
   ============================================ */
.modules-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
  gap: 14px;
}

.module-card {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: 18px;
}

.module-focus {
  border-color: rgba(34,197,94,0.2);
  background: rgba(34,197,94,0.03);
}

.module-header {
  display: flex;
  align-items: center;
  gap: 12px;
  margin-bottom: 10px;
}

.module-icon {
  font-size: 20px;
}

.module-name {
  font-size: 14px;
  font-weight: 700;
  color: var(--text-primary);
}

.module-status {
  font-size: 11px;
  font-weight: 600;
  padding: 2px 8px;
  border-radius: 20px;
}

.status-active-sm  { background: rgba(34,197,94,0.12);  color: #4ade80; }
.status-done-sm    { background: rgba(59,130,246,0.12); color: #60a5fa; }
.status-waiting-sm { background: rgba(255,255,255,0.05); color: var(--text-muted); }

.module-desc {
  font-size: 12px;
  color: var(--text-secondary);
  line-height: 1.55;
  margin-bottom: 12px;
}

.module-subs {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.sub-item {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 6px 8px;
  border-radius: 6px;
  background: rgba(255,255,255,0.03);
  font-size: 12px;
  color: var(--text-secondary);
}

.sub-item i { font-size: 11px; color: var(--text-muted); }

.sub-focus {
  background: rgba(239,68,68,0.08);
  color: var(--text-primary);
}

.sub-done {
  color: var(--text-muted);
  text-decoration: line-through;
}

.sub-badge {
  margin-left: auto;
  font-size: 10px;
  font-weight: 700;
  background: rgba(239,68,68,0.12);
  color: var(--accent-red);
  padding: 2px 7px;
  border-radius: 20px;
}

.sub-badge-neutral {
  margin-left: auto;
  font-size: 10px;
  color: var(--text-muted);
}

/* ============================================
   ROADMAP
   ============================================ */
.roadmap {
  display: flex;
  flex-direction: column;
  gap: 0;
  position: relative;
  padding-left: 32px;
  margin-bottom: 28px;
}

.roadmap::before {
  content: '';
  position: absolute;
  left: 15px;
  top: 24px;
  bottom: 24px;
  width: 2px;
  background: var(--border);
}

.roadmap-item {
  display: flex;
  gap: 16px;
  align-items: flex-start;
  padding: 14px 0;
  position: relative;
}

.roadmap-marker {
  position: absolute;
  left: -32px;
  top: 14px;
  width: 28px; height: 28px;
  border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  font-size: 12px;
  font-weight: 700;
  flex-shrink: 0;
  z-index: 1;
}

.done-marker   { background: rgba(34,197,94,0.15);  color: #4ade80; border: 2px solid rgba(34,197,94,0.3); }
.active-marker { background: var(--accent-red);      color: #fff;    box-shadow: 0 0 16px rgba(239,68,68,0.4); }
.pending-marker{ background: var(--bg-card);         color: var(--text-muted); border: 2px solid var(--border); }

.roadmap-label {
  display: block;
  font-size: 10px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  margin-bottom: 3px;
}

.done-label    { color: #4ade80; }
.active-label  { color: var(--accent-red); }
.pending-label { color: var(--text-muted); }

.roadmap-text {
  font-size: 13px;
  color: var(--text-secondary);
}

/* ============================================
   FRENTES (Revest Tudo)
   ============================================ */
.frentes-list {
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.frente-item {
  display: flex;
  gap: 16px;
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: 16px 18px;
  align-items: flex-start;
  transition: all var(--transition);
}

.frente-item:hover {
  border-color: rgba(249,115,22,0.2);
  background: var(--bg-card-hover);
}

.frente-alta  { border-left: 3px solid var(--accent-red); }
.frente-media { border-left: 3px solid var(--accent-orange); }

.frente-priority {
  font-size: 10px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--accent-red);
  white-space: nowrap;
  margin-top: 3px;
}

.frente-priority-media { color: var(--accent-orange); }

.frente-name {
  font-size: 14px;
  font-weight: 700;
  color: var(--text-primary);
  margin-bottom: 6px;
  display: flex;
  align-items: center;
  gap: 8px;
}

.frente-name i { font-size: 13px; color: var(--text-muted); }

.frente-desc {
  font-size: 12px;
  color: var(--text-secondary);
  line-height: 1.55;
  margin-bottom: 8px;
}

.frente-next {
  display: flex;
  align-items: center;
  gap: 6px;
  font-size: 12px;
  color: var(--accent-orange);
  font-weight: 600;
}

.frente-next i { font-size: 10px; }

/* ============================================
   BACKLOG (Zé Vendas)
   ============================================ */
.backlog-section {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.backlog-group {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  overflow: hidden;
}

.backlog-group-header {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 10px 16px;
  font-size: 12px;
  font-weight: 700;
  border-bottom: 1px solid var(--border);
}

.bg-red-header    { color: var(--accent-red);    background: rgba(239,68,68,0.06); }
.bg-yellow-header { color: var(--accent-yellow); background: rgba(251,191,36,0.05); }
.bg-gray-header   { color: var(--text-secondary); background: rgba(255,255,255,0.02); }

.backlog-item {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 10px 16px;
  border-bottom: 1px solid var(--border-light);
  font-size: 13px;
  color: var(--text-secondary);
}

.backlog-item:last-child { border-bottom: none; }

.backlog-dot {
  width: 7px; height: 7px;
  border-radius: 50%;
  flex-shrink: 0;
}

.backlog-text { flex: 1; }

.backlog-tag {
  font-size: 10px;
  font-weight: 700;
  padding: 2px 7px;
  border-radius: 20px;
}

/* ============================================
   AIME KOMATSU
   ============================================ */
.aime-hero {
  text-align: center;
  padding: 40px 32px;
  background: linear-gradient(135deg, rgba(168,85,247,0.08), rgba(168,85,247,0.02));
  border: 1px solid rgba(168,85,247,0.15);
  border-radius: var(--radius);
  margin-bottom: 28px;
}

.aime-symbol {
  font-size: 48px;
  color: var(--accent-purple);
  line-height: 1;
  margin-bottom: 16px;
}

.aime-quote {
  font-size: 22px;
  font-weight: 700;
  color: var(--text-primary);
  font-style: italic;
  margin-bottom: 12px;
  letter-spacing: -0.01em;
}

.aime-subdesc {
  font-size: 14px;
  color: var(--text-secondary);
  max-width: 500px;
  margin: 0 auto;
  line-height: 1.6;
}

.aime-note {
  display: flex;
  align-items: flex-start;
  gap: 12px;
  background: rgba(168,85,247,0.06);
  border: 1px solid rgba(168,85,247,0.15);
  border-radius: var(--radius);
  padding: 16px 18px;
  margin-top: 24px;
}

.aime-note i {
  color: var(--accent-purple);
  font-size: 16px;
  margin-top: 2px;
  flex-shrink: 0;
}

.aime-note p {
  font-size: 13px;
  color: var(--text-secondary);
  line-height: 1.6;
}

/* ============================================
   RESPONSIVE
   ============================================ */
@media (max-width: 1024px) {
  .metrics-row { grid-template-columns: repeat(2, 1fr); }
  .priority-columns { grid-template-columns: 1fr; }
}

@media (max-width: 768px) {
  .mobile-topbar { display: flex; }
  
  .sidebar {
    transform: translateX(-100%);
    transition: transform 0.25s ease;
  }

  .sidebar.open { transform: translateX(0); }

  .main-content {
    margin-left: 0;
    padding: 16px;
    padding-top: calc(var(--topbar-h) + 16px);
  }

  .projects-grid { grid-template-columns: 1fr; }
  .metrics-row { grid-template-columns: repeat(2, 1fr); }
  .focus-banner { flex-direction: column; align-items: flex-start; gap: 16px; }
  .page-header { flex-direction: column; align-items: flex-start; }
  .page-title { font-size: 20px; }
  .modules-grid { grid-template-columns: 1fr; }

  .priority-columns {
    grid-template-columns: 1fr;
  }
}

@media (max-width: 480px) {
  .metrics-row { grid-template-columns: 1fr 1fr; gap: 10px; }
  .metric-card { padding: 12px; }
  .metric-value { font-size: 17px; }
}

/* ============================================
   GUIA RÁPIDO
   ============================================ */
.guide-banner {
  background: rgba(59,130,246,0.06);
  border: 1px solid rgba(59,130,246,0.18);
  border-radius: var(--radius);
  padding: 18px 22px;
  margin-bottom: 22px;
}

.guide-title {
  font-size: 12px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: #60a5fa;
  margin-bottom: 14px;
  display: flex;
  align-items: center;
  gap: 7px;
}

.guide-steps {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 12px;
  margin-bottom: 16px;
}

.guide-step {
  display: flex;
  align-items: flex-start;
  gap: 10px;
}

.guide-num {
  width: 22px; height: 22px;
  border-radius: 50%;
  background: rgba(59,130,246,0.18);
  color: #60a5fa;
  font-size: 11px;
  font-weight: 800;
  display: flex; align-items: center; justify-content: center;
  flex-shrink: 0;
  margin-top: 1px;
}

.guide-text {
  font-size: 12px;
  color: var(--text-secondary);
  line-height: 1.55;
}

.guide-text strong { color: var(--text-primary); }

.guide-cta {
  display: inline-flex;
  align-items: center;
  gap: 7px;
  padding: 8px 16px;
  background: rgba(59,130,246,0.12);
  color: #60a5fa;
  border: 1px solid rgba(59,130,246,0.25);
  border-radius: var(--radius-sm);
  font-size: 12px;
  font-weight: 700;
  cursor: pointer;
  transition: all var(--transition);
}

.guide-cta:hover {
  background: rgba(59,130,246,0.2);
  border-color: rgba(59,130,246,0.4);
}

@media (max-width: 768px) {
  .guide-steps { grid-template-columns: 1fr 1fr; }
}
@media (max-width: 480px) {
  .guide-steps { grid-template-columns: 1fr; }
}

/* ============================================
   PROMPTS
   ============================================ */
.prompt-instruction {
  display: flex;
  align-items: flex-start;
  gap: 12px;
  background: rgba(59,130,246,0.07);
  border: 1px solid rgba(59,130,246,0.2);
  border-radius: var(--radius);
  padding: 14px 18px;
  margin-bottom: 24px;
  font-size: 13px;
  color: var(--text-secondary);
  line-height: 1.6;
}

.prompt-instruction i {
  color: #60a5fa;
  font-size: 15px;
  margin-top: 2px;
  flex-shrink: 0;
}

.prompt-instruction strong { color: var(--text-primary); }

.prompts-list {
  display: flex;
  flex-direction: column;
  gap: 14px;
}

.prompt-card {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  overflow: hidden;
  transition: border-color var(--transition);
}

.prompt-card:hover { border-color: rgba(255,255,255,0.12); }

.prompt-card-final {
  border-color: rgba(239,68,68,0.2);
  background: rgba(239,68,68,0.03);
}

.prompt-card-header {
  display: flex;
  align-items: center;
  gap: 14px;
  padding: 16px 20px;
  border-bottom: 1px solid var(--border);
}

.prompt-order {
  width: 32px; height: 32px;
  border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  font-size: 13px; font-weight: 800;
  flex-shrink: 0;
}

.order-active  { background: var(--accent-red); color: #fff; box-shadow: 0 0 14px rgba(239,68,68,0.35); }
.order-pending { background: rgba(255,255,255,0.06); color: var(--text-muted); border: 2px solid var(--border); }
.order-final   { background: rgba(239,68,68,0.12); color: var(--accent-red); font-size: 14px; }

.prompt-meta { flex: 1; min-width: 0; }

.prompt-title {
  font-size: 14px;
  font-weight: 700;
  color: var(--text-primary);
  margin-bottom: 3px;
}

.prompt-status {
  font-size: 11px;
  font-weight: 600;
  padding: 2px 8px;
  border-radius: 20px;
}

.btn-copy {
  display: flex;
  align-items: center;
  gap: 7px;
  padding: 8px 16px;
  background: rgba(255,255,255,0.06);
  color: var(--text-secondary);
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  font-size: 12px;
  font-weight: 600;
  cursor: pointer;
  white-space: nowrap;
  transition: all var(--transition);
  flex-shrink: 0;
}

.btn-copy:hover {
  background: var(--accent-red);
  color: #fff;
  border-color: var(--accent-red);
}

.btn-copy.copied {
  background: rgba(34,197,94,0.12);
  color: #4ade80;
  border-color: rgba(34,197,94,0.3);
}

.prompt-context {
  padding: 10px 20px;
  font-size: 12px;
  color: var(--text-muted);
  background: rgba(255,255,255,0.02);
  border-bottom: 1px solid var(--border-light);
  font-style: italic;
}

.prompt-box {
  padding: 16px 20px;
  font-size: 12px;
  line-height: 1.7;
  color: var(--text-secondary);
  white-space: pre-wrap;
  font-family: 'Inter', monospace;
  max-height: 220px;
  overflow-y: auto;
  background: var(--bg-base);
  cursor: text;
  user-select: text;
}

@media (max-width: 768px) {
  .prompt-card-header { flex-wrap: wrap; gap: 10px; }
  .btn-copy { width: 100%; justify-content: center; }
}

/* ============================================
   FORNECEDORES — BLOCOS DETALHADOS
   ============================================ */

/* Onde cadastrar — lista de caminhos */
.onb-where-list {
  display: flex;
  flex-direction: column;
  gap: 10px;
  margin: 14px 0;
}
.onb-where-item {
  display: flex;
  align-items: flex-start;
  gap: 14px;
  background: rgba(255,255,255,0.03);
  border: 1px solid rgba(255,255,255,0.06);
  border-radius: 10px;
  padding: 12px 16px;
}
.onb-where-num {
  background: rgba(249,115,22,0.15);
  color: #f97316;
  font-size: 11px;
  font-weight: 700;
  padding: 3px 8px;
  border-radius: 20px;
  white-space: nowrap;
  flex-shrink: 0;
  margin-top: 2px;
}
.onb-where-sub {
  display: block;
  font-size: 12px;
  color: var(--text-muted);
  margin-top: 3px;
}

/* Campos — grid de referência */
.onb-fields-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
  gap: 10px;
  margin: 14px 0;
}
.onb-field {
  display: flex;
  flex-direction: column;
  gap: 4px;
  padding: 12px 14px;
  border-radius: 10px;
  border: 1px solid rgba(255,255,255,0.06);
  background: rgba(255,255,255,0.025);
}
.onb-field-required { border-left: 3px solid var(--accent-red); }
.onb-field-recommended { border-left: 3px solid var(--accent-yellow); }
.onb-field-optional { border-left: 3px solid rgba(255,255,255,0.1); }

.onb-field-label {
  font-size: 13px;
  font-weight: 600;
  color: var(--text-primary);
}
.onb-field-ex {
  font-size: 11px;
  color: var(--text-muted);
  font-style: italic;
}
.onb-field-req {
  font-size: 10px;
  font-weight: 700;
  color: var(--accent-red);
  text-transform: uppercase;
  letter-spacing: 0.06em;
}
.onb-field-rec {
  font-size: 10px;
  font-weight: 700;
  color: var(--accent-yellow);
  text-transform: uppercase;
  letter-spacing: 0.06em;
}
.onb-field-opt {
  font-size: 10px;
  font-weight: 700;
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 0.06em;
}

/* Fornecedores de demo — cards prontos */
.onb-demo-suppliers {
  display: flex;
  flex-direction: column;
  gap: 10px;
  margin: 14px 0;
}
.onb-supplier-card {
  display: flex;
  align-items: flex-start;
  gap: 14px;
  background: rgba(249,115,22,0.04);
  border: 1px solid rgba(249,115,22,0.15);
  border-radius: 12px;
  padding: 14px 16px;
}
.onb-supplier-icon {
  width: 40px;
  height: 40px;
  border-radius: 10px;
  background: rgba(249,115,22,0.15);
  color: #f97316;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 16px;
  flex-shrink: 0;
}
.onb-supplier-info {
  display: flex;
  flex-direction: column;
  gap: 3px;
}
.onb-supplier-info strong {
  font-size: 14px;
  color: var(--text-primary);
}
.onb-supplier-info span {
  font-size: 12px;
  color: var(--text-secondary);
}

/* Fluxo de conexão */
.onb-flow {
  display: flex;
  align-items: center;
  gap: 10px;
  flex-wrap: wrap;
  margin: 14px 0;
  padding: 16px;
  background: rgba(255,255,255,0.02);
  border-radius: 12px;
  border: 1px solid rgba(255,255,255,0.06);
}
.onb-flow-step {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8px;
  text-align: center;
  flex: 1;
  min-width: 90px;
}
.onb-flow-icon {
  width: 44px;
  height: 44px;
  border-radius: 12px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 18px;
}
.onb-flow-text {
  font-size: 12px;
  color: var(--text-secondary);
  line-height: 1.4;
}
.onb-flow-text strong {
  display: block;
  color: var(--text-primary);
  font-size: 12px;
  margin-bottom: 2px;
}
.onb-flow-arrow {
  font-size: 18px;
  color: var(--text-muted);
  flex-shrink: 0;
}

/* ============================================
   ROTEIRO DE DEMO
   ============================================ */

.demo-timeline {
  display: flex;
  flex-direction: column;
  gap: 0;
  margin-top: 1.5rem;
}

.demo-scene {
  display: flex;
  gap: 0;
}

.demo-scene-marker {
  display: flex;
  flex-direction: column;
  align-items: center;
  flex-shrink: 0;
  width: 56px;
}

.demo-scene-num {
  width: 36px;
  height: 36px;
  border-radius: 50%;
  background: var(--accent-red);
  color: #fff;
  font-size: 15px;
  font-weight: 700;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  box-shadow: 0 0 0 4px rgba(239,68,68,0.12);
}

.demo-scene-line {
  width: 2px;
  flex: 1;
  background: rgba(239,68,68,0.2);
  margin: 6px 0;
  min-height: 24px;
}
.demo-scene-line-last {
  width: 2px;
  flex: 1;
  background: transparent;
  margin: 6px 0;
}

.demo-scene-body {
  flex: 1;
  padding: 0 0 32px 20px;
}

.demo-scene-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 12px;
  margin-bottom: 14px;
  flex-wrap: wrap;
}

.demo-scene-meta {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.demo-scene-title {
  font-size: 15px;
  font-weight: 700;
  color: var(--text-primary);
}

.demo-scene-time {
  font-size: 12px;
  color: var(--text-muted);
  display: flex;
  align-items: center;
  gap: 5px;
}

.demo-scene-screen {
  background: rgba(239,68,68,0.1);
  color: #ef4444;
  font-size: 11px;
  font-weight: 600;
  padding: 4px 10px;
  border-radius: 20px;
  white-space: nowrap;
  border: 1px solid rgba(239,68,68,0.2);
  flex-shrink: 0;
}

.demo-fala {
  background: rgba(255,255,255,0.03);
  border: 1px solid rgba(255,255,255,0.07);
  border-radius: 12px;
  padding: 14px 16px;
  margin-bottom: 14px;
}

.demo-fala-label {
  font-size: 10px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--text-muted);
  display: flex;
  align-items: center;
  gap: 6px;
  margin-bottom: 8px;
}

.demo-fala-text {
  font-size: 14px;
  color: var(--text-secondary);
  line-height: 1.6;
  font-style: italic;
}

.demo-actions {
  display: flex;
  flex-direction: column;
  gap: 7px;
  margin-bottom: 12px;
}

.demo-action-item {
  display: flex;
  align-items: flex-start;
  gap: 10px;
  font-size: 13px;
  color: var(--text-secondary);
  line-height: 1.5;
}

.demo-action-item i {
  color: var(--text-muted);
  margin-top: 2px;
  flex-shrink: 0;
  font-size: 12px;
  width: 14px;
  text-align: center;
}

.demo-action-item i.fa-mouse-pointer { color: #60a5fa; }
.demo-action-item i.fa-eye { color: #4ade80; }
.demo-action-item i.fa-comments { color: #c084fc; }

.demo-highlight {
  display: flex;
  align-items: flex-start;
  gap: 10px;
  background: rgba(251,191,36,0.06);
  border: 1px solid rgba(251,191,36,0.2);
  border-radius: 10px;
  padding: 10px 14px;
  font-size: 13px;
  color: var(--text-secondary);
  margin-top: 6px;
}

.demo-highlight i {
  color: var(--accent-yellow);
  flex-shrink: 0;
  margin-top: 2px;
}

.demo-highlight strong {
  color: var(--accent-yellow);
}

/* ============================================
   CORRIGIR O APP — Fix Cards
   ============================================ */

.fix-intro {
  display: flex;
  align-items: flex-start;
  gap: 12px;
  background: rgba(239,68,68,0.06);
  border: 1px solid rgba(239,68,68,0.18);
  border-radius: var(--radius);
  padding: 14px 18px;
  margin-bottom: 24px;
  font-size: 13px;
  color: var(--text-secondary);
  line-height: 1.6;
}
.fix-intro i { color: var(--accent-red); font-size: 15px; flex-shrink: 0; margin-top: 2px; }
.fix-intro strong { color: var(--text-primary); }

.fix-card {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  margin-bottom: 16px;
  overflow: hidden;
}
.fix-critical { border-left: 3px solid #ef4444; }
.fix-warning  { border-left: 3px solid #fbbf24; }
.fix-info     { border-left: 3px solid #3b82f6; }

.fix-card-header {
  padding: 18px 20px 14px;
  border-bottom: 1px solid var(--border-light);
}

.fix-badge {
  display: inline-flex;
  align-items: center;
  gap: 5px;
  font-size: 10px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  padding: 3px 10px;
  border-radius: 20px;
  margin-bottom: 10px;
}
.fix-badge-critical { background: rgba(239,68,68,0.15); color: #f87171; border: 1px solid rgba(239,68,68,0.25); }
.fix-badge-warning  { background: rgba(251,191,36,0.12); color: #fbbf24; border: 1px solid rgba(251,191,36,0.2); }
.fix-badge-info     { background: rgba(59,130,246,0.12); color: #60a5fa; border: 1px solid rgba(59,130,246,0.2); }

.fix-title {
  font-size: 15px;
  font-weight: 700;
  color: var(--text-primary);
  margin-bottom: 8px;
  line-height: 1.4;
}

.fix-desc {
  font-size: 13px;
  color: var(--text-secondary);
  line-height: 1.6;
  margin-bottom: 10px;
}

.fix-impact {
  display: flex;
  align-items: flex-start;
  gap: 8px;
  background: rgba(255,255,255,0.03);
  border-radius: 8px;
  padding: 9px 12px;
  font-size: 12px;
  color: var(--text-muted);
  line-height: 1.5;
}
.fix-impact i { color: var(--text-muted); flex-shrink: 0; margin-top: 2px; }
.fix-impact strong { color: var(--text-secondary); }

.fix-prompt-wrap {
  padding: 16px 20px;
  background: var(--bg-base);
}

.fix-prompt-label {
  font-size: 10px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--text-muted);
  margin-bottom: 10px;
  display: flex;
  align-items: center;
  gap: 6px;
}

.btn-copy-fix {
  margin-top: 12px;
  display: inline-flex;
  align-items: center;
  gap: 7px;
  padding: 9px 18px;
  background: var(--accent-red);
  color: #fff;
  border: none;
  border-radius: var(--radius-sm);
  font-size: 12px;
  font-weight: 700;
  cursor: pointer;
  transition: all var(--transition);
}
.btn-copy-fix:hover { background: #dc2626; box-shadow: 0 0 16px rgba(239,68,68,0.35); }
.btn-copy-fix.copied { background: rgba(34,197,94,0.15); color: #4ade80; box-shadow: none; }

.fix-note {
  display: flex;
  align-items: flex-start;
  gap: 8px;
  margin-top: 12px;
  font-size: 12px;
  color: var(--text-muted);
  line-height: 1.55;
}
.fix-note i { color: var(--accent-yellow); flex-shrink: 0; margin-top: 2px; }
.fix-note code {
  background: rgba(255,255,255,0.07);
  padding: 1px 5px;
  border-radius: 4px;
  font-size: 11px;
  color: #fbbf24;
}

/* Sequência recomendada */
.fix-sequence {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: 20px 22px;
  margin-top: 8px;
}
.fix-seq-title {
  font-size: 12px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--text-muted);
  margin-bottom: 18px;
  display: flex;
  align-items: center;
  gap: 7px;
}
.fix-seq-steps {
  display: flex;
  flex-direction: column;
  gap: 0;
}
.fix-seq-step {
  display: flex;
  align-items: center;
  gap: 14px;
  background: rgba(255,255,255,0.03);
  border: 1px solid rgba(255,255,255,0.06);
  border-radius: 10px;
  padding: 12px 14px;
}
.fix-seq-num {
  width: 28px; height: 28px;
  border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  color: #fff;
  font-size: 12px;
  font-weight: 800;
  flex-shrink: 0;
}
.fix-seq-text { font-size: 13px; color: var(--text-secondary); line-height: 1.4; }
.fix-seq-text strong { color: var(--text-primary); }
.fix-seq-arrow {
  display: flex;
  justify-content: center;
  padding: 5px 0;
  color: var(--text-muted);
  font-size: 12px;
}

/* Checklist dentro de fix-card */
.fix-checklist {
  display: flex;
  flex-direction: column;
  gap: 10px;
  margin-bottom: 4px;
}

.fix-check-item {
  display: flex;
  align-items: flex-start;
  gap: 12px;
  background: rgba(255,255,255,0.03);
  border: 1px solid rgba(255,255,255,0.06);
  border-radius: 10px;
  padding: 11px 14px;
}

.fix-check-num {
  width: 24px; height: 24px;
  border-radius: 50%;
  background: rgba(34,197,94,0.15);
  color: #4ade80;
  font-size: 12px;
  font-weight: 800;
  display: flex; align-items: center; justify-content: center;
  flex-shrink: 0;
  margin-top: 1px;
}

.fix-check-item > div {
  display: flex;
  flex-direction: column;
  gap: 3px;
}

.fix-check-item > div strong {
  font-size: 13px;
  color: var(--text-primary);
  font-weight: 600;
}

.fix-check-sub {
  font-size: 12px;
  color: var(--text-muted);
  line-height: 1.5;
}

.fix-card code {
  background: rgba(255,255,255,0.07);
  padding: 1px 5px;
  border-radius: 4px;
  font-size: 11px;
  color: #fbbf24;
  font-family: 'Courier New', monospace;
}

/* Tabela de diagnóstico de alertas */
.alert-diagnosis-table {
  display: flex;
  flex-direction: column;
  gap: 3px;
  width: 100%;
}

.adt-row {
  display: grid;
  grid-template-columns: 1.6fr 2fr 1fr;
  gap: 8px;
  padding: 7px 10px;
  border-radius: 7px;
  font-size: 12px;
  align-items: center;
}

.adt-header {
  background: rgba(255,255,255,0.04);
  font-size: 10px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.07em;
  color: var(--text-muted);
}

.adt-row:not(.adt-header) {
  background: rgba(255,255,255,0.02);
  border: 1px solid rgba(255,255,255,0.04);
  color: var(--text-secondary);
}

.adt-row code {
  font-size: 10px;
  background: rgba(255,255,255,0.06);
  padding: 1px 5px;
  border-radius: 4px;
  color: #fcd34d;
  font-family: 'Courier New', monospace;
}

.adt-ok     { color: #4ade80; font-size: 11px; font-weight: 600; }
.adt-fixed  { color: #60a5fa; font-size: 11px; font-weight: 600; }
.adt-new    { color: #f97316; font-size: 11px; font-weight: 600; }

@media (max-width: 640px) {
  .adt-row { grid-template-columns: 1fr; gap: 3px; }
  .adt-header { display: none; }
}

/* ============================================
   PERFIS & SENHAS
   ============================================ */

/* Grid de cards de perfil */
.profiles-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
  gap: 14px;
  margin-bottom: 2rem;
}

.profile-card {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: 18px 20px;
  transition: border-color var(--transition), transform var(--transition);
}

.profile-card:hover {
  border-color: rgba(255,255,255,0.13);
  transform: translateY(-1px);
}

.profile-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 12px;
}

.profile-avatar {
  width: 42px; height: 42px;
  border-radius: 12px;
  display: flex; align-items: center; justify-content: center;
  font-size: 17px;
  color: #fff;
  flex-shrink: 0;
}

.profile-name {
  font-size: 14px;
  font-weight: 700;
  color: var(--text-primary);
  margin-bottom: 4px;
}

.profile-role-badge {
  font-size: 10px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.07em;
  padding: 2px 8px;
  border-radius: 20px;
}

.profile-desc {
  font-size: 12px;
  color: var(--text-secondary);
  line-height: 1.55;
  margin-bottom: 10px;
}

.profile-sees {
  font-size: 11px;
  color: var(--text-muted);
  background: rgba(255,255,255,0.03);
  border-radius: 8px;
  padding: 8px 10px;
  line-height: 1.5;
  display: flex;
  gap: 6px;
}

.profile-sees i {
  color: var(--text-muted);
  flex-shrink: 0;
  margin-top: 2px;
  font-size: 10px;
}

/* Passos de senha */
.password-step {
  display: flex;
  align-items: flex-start;
  gap: 12px;
}

.pwd-step-num {
  width: 24px; height: 24px;
  border-radius: 50%;
  background: rgba(239,68,68,0.15);
  color: var(--accent-red);
  font-size: 12px;
  font-weight: 800;
  display: flex; align-items: center; justify-content: center;
  flex-shrink: 0;
  margin-top: 1px;
}

.pwd-step-text {
  font-size: 13px;
  color: var(--text-secondary);
  line-height: 1.55;
}

.pwd-step-text strong { color: var(--text-primary); }
.pwd-step-text code {
  background: rgba(255,255,255,0.07);
  padding: 1px 5px;
  border-radius: 4px;
  font-size: 11px;
  color: #fbbf24;
}

/* Bloco SQL */
.sql-block {
  background: var(--bg-base);
  border: 1px solid rgba(249,115,22,0.25);
  border-radius: var(--radius-sm);
  padding: 14px 16px;
  font-family: 'Courier New', Courier, monospace;
  font-size: 12px;
  color: #fcd34d;
  white-space: pre;
  overflow-x: auto;
  line-height: 1.65;
}

/* Tabela de senhas */
.passwords-table {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  overflow: hidden;
  margin-bottom: 2rem;
}

.pwd-table-header {
  display: grid;
  grid-template-columns: 1.4fr 2fr 2fr 0.9fr;
  gap: 0;
  padding: 10px 18px;
  background: rgba(255,255,255,0.03);
  border-bottom: 1px solid var(--border);
  font-size: 10px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--text-muted);
}

.pwd-row {
  display: grid;
  grid-template-columns: 1.4fr 2fr 2fr 0.9fr;
  gap: 0;
  padding: 12px 18px;
  border-bottom: 1px solid var(--border-light);
  align-items: center;
  transition: background var(--transition);
}

.pwd-row:last-child { border-bottom: none; }
.pwd-row:hover { background: rgba(255,255,255,0.02); }

.pwd-profile {
  font-size: 13px;
  font-weight: 600;
  color: var(--text-primary);
  display: flex;
  align-items: center;
  gap: 8px;
}

.pwd-email, .pwd-pass {
  font-size: 11px;
  font-family: 'Courier New', monospace;
  background: rgba(255,255,255,0.05);
  padding: 3px 7px;
  border-radius: 5px;
  color: #93c5fd;
  width: fit-content;
  user-select: all;
}

.pwd-pass { color: #86efac; }

.pwd-perm {
  font-size: 10px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.06em;
  padding: 3px 8px;
  border-radius: 20px;
  width: fit-content;
}

.perm-full  { background: rgba(239,68,68,0.12); color: #ef4444; }
.perm-mid   { background: rgba(59,130,246,0.12); color: #60a5fa; }
.perm-low   { background: rgba(100,116,139,0.12); color: #94a3b8; }

@media (max-width: 768px) {
  .profiles-grid { grid-template-columns: 1fr; }
  .pwd-table-header { display: none; }
  .pwd-row {
    grid-template-columns: 1fr;
    gap: 6px;
    padding: 14px 16px;
  }
  .pwd-email, .pwd-pass { font-size: 12px; }
}

/* Grid de dados para ter na ponta da língua */
.demo-data-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
  gap: 14px;
  margin-top: 1rem;
}

.demo-data-card {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: 12px;
  padding: 18px;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.demo-data-icon {
  font-size: 20px;
  color: var(--accent-red);
}

.demo-data-label {
  font-size: 11px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.07em;
  color: var(--text-muted);
}

.demo-data-value {
  font-size: 13px;
  font-weight: 500;
  color: var(--text-primary);
  line-height: 1.5;
}

/* Responsivo */
@media (max-width: 600px) {
  .onb-fields-grid { grid-template-columns: 1fr; }
  .onb-flow { flex-direction: column; align-items: flex-start; }
  .onb-flow-step { flex-direction: row; text-align: left; min-width: unset; }
  .onb-flow-arrow { transform: rotate(90deg); }
  .demo-data-grid { grid-template-columns: 1fr; }
  .demo-scene-header { flex-direction: column; }
  .demo-scene-screen { align-self: flex-start; }
}
